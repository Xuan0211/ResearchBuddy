# Coding

Qualitative coding and literature screening projects.

- `Project/{id}/` — One directory per coding project
- `resources/` — Supporting files
- `utils.read_only/` — Utility scripts (system-managed)
