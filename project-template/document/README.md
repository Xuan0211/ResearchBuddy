# Document

Long-form notes, drafts, and shared project documents.

- `docs/` — One Markdown file per document (`{id}.md`)
- `images/` — Embedded images
- `resources/` — Supporting files
- `utils.read_only/` — Utility scripts (system-managed)
