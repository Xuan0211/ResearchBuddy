# ResearchBuddy System Directory

ResearchBuddy owns this directory. Agents and teammates can read it for context,
but should avoid hand-editing generated indexes, sync state, locks, and adapter
metadata unless they are intentionally repairing the workspace.

